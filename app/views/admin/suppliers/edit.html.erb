<%= content_for :title do %>New Supplier<% end %>

<div class="container" id="generic">
  <div class="row-fluid">
    <h1 id="h1-override">You are editing <%= @supplier.name %></h1>
    <div class="hero-unit">
      <div class="errors">

      </div>
      <div class="row-fluid">
        <%= simple_form_for (@supplier), url: update_supplier_path(@supplier)  do |f| %>
            <%= render :partial => 'newedit', :locals => { :f => f}  %>
        <% end %>
      </div>
    </div>
  </div>
</div>
